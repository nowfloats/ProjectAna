version: '3'
services:
  web:
    build: .
    ports:
     - "80:80"
    depends_on:
      - mongodb
  mongodb:
    image: "mongo:latest"
    ports:
      - "27017:27017"
    volumes:
      - db-data:/data/db

volumes:
  db-data: