{
  "Logging": {
    "IncludeScopes": false,
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "FileLogging": {
    "PathFormat": "Logs/log-{Date}.txt",
    "LogLevel": {
      "Default": "Debug",
      "Microsoft": "Information"
    }
  },
  "Settings": {
    "StudioDistFolder": "", //Path to folder where studio zips are put when an update has to be pushed, in the format: {major}.{minor}.{build}.{rev}.zip
    "MaxCapTimeTakenToType": 3000,
    "BaseTimeTakenToTypePerChar": 30,
    "VariableTimeTakenToTypePerChar": 10
  },
  "DatabaseConnectionSettings": {
    "ConnectionString": "", //MongoDB Connection String
    "DatabaseName": "", //Mongo Database name
    "ActivityEventLogCollectionName": "", //New Empty Collection Name A
    "ProjectsCollectionName": "", //New Empty Collection Name B
    "ChatFlowPacksCollectionName": "", //New Empty Collection Name C
    "ChatFlowPacksBackupCollectionName": "", //New Empty Collection Name D,
    "MaxBackups": 10 //No of backups to maintain
  },
  "AgentChatSettings": {
    "DefaultAgentUserName": "", //Fallback username if no agent is online
    "APIBase": "", //RocketChat Endpoint, Should end with '/'
    "APIUserName": "", //RocketChat Admin Username
    "APIPassword": "" //RocketChat Admin Password
  },
  "LiveClientSocketsServerSettings": {
    "Server": "", //Sockets Server Endpoint, Should end with '/'
    "AuthBasic": "" //Sockets Server Auth Header without 'Basic ' part
  },
  "BasicAuth": {
    //Use the same in the studio. Leaving them empty will enable public write access to your chat server!
    "APIKey": "", //Set an api key here
    "APISecret": "" //Set an api secret here.
  }
}
