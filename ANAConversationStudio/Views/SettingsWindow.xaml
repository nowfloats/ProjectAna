<Window x:Class="ANAConversationStudio.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:ANAConversationStudio.Views"
        mc:Ignorable="d"
        Title="Settings" Height="500" Width="600">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <xctk:PropertyGrid Margin="10" x:Name="SettingsEditor" SelectedObject="{Binding Config}"/>
        <Button Content="Save" Click="SaveClick" Grid.Row="1" Margin="10" HorizontalAlignment="Center" Padding="30,5"/>
    </Grid>
</Window>
